#include <stdio.h>
int factorial(int n); // factorial function

int pselect(int n, int k); // p select

int cselect(int n, int k); // c select

float convertPercent(float n, float min, float max); // convert to percentage

float percentage(float n, float k); // what percent of n is k
