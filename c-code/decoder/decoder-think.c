#include <stdio.h>

int main() 
{
	char secretcode="i like meatbol";
	int counter = 0;
	char cascii;
	cascii = (char) counter;
	if  (cascii != secretcode) 
	{
		cascii = (char) counter;
		printf("secret code is %c",counter);
	}
	//printf(cascii);
}
